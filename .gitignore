target/
*.png
# The phase gates are reviewed on screenshots, so the docs' images are kept.
!docs/images/*.png
!assets/icon-*.png
.DS_Store
__pycache__/
# Local secrets for testing (owner-provided API key); never committed.
.dev.env
.env
