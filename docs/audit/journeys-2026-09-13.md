# User journeys — 2026-09-13 (owner round 3)

Two halves. **J1** is everything `--replay`, `--no-connect` and `--steps` can
drive: free, scripted, and rerunnable. **J2** is the live half, which needs a
real wire.

## J1 — scripted, free

`scripts/journeys.sh <out-dir>` runs them all. Each journey is one `--steps`
script, run **twice from two fresh state dirs**, and it passes only when both
runs produce a capture *and* the two are byte-identical. That is the strongest
pass/fail a screenshot can give on its own: it proves the journey reaches the
same place every time, which is what catches an ordering or timing regression
that a single capture would hide. The capture path is in the report beside
each result.

Everything runs under `HARNESS_DETERMINISTIC=1`. Park the pointer outside the
top-left 1440×900 before running: a capture renders there, and a hovered row
leaks into it.

Results: `<out-dir>/journeys.txt`, reproduced in §3 below.

### What J1 covers

| group | journeys |
|---|---|
| boot and adoption | the hero with no project; adopt one, two, three |
| switching | the header's project menu; the Projects palette |
| project edits | recolour; a named project's group menu; the removal dialog |
| session rows | open, pin, rename-in-place, the archive dialog, archived, show-archived, hidden, empty |
| grouping and search | group by date, group by project, search across projects, a search that finds nothing |
| menus | overflow, view, account, model, effort, mode, the composer `+`, the `/` popover, the `@` popover, the command palette |
| sidebar shape | collapsed (the rail), 200 px, 420 px |
| transcript | head, middle, tail, every tool group expanded, a draft in the composer |
| blocks | approvals pending / chosen / settled, a question, markdown, tool shapes, a tool group, an error and its retry, todos, reasoning, an empty session, "Finishing up" |
| login | choose, device, API key, error |

### What J1 cannot cover, and why

These are in the brief's list but have no `--steps` verb, and the app has no
way to move the pointer or synthesise a key from a script:

- **Dismissing a menu by Escape and by an outside click.** Both are pointer or
  key paths. The *code* for the outside-click catcher landed in round 2 (P4)
  and every menu has one; this round could not re-prove it on screen. A verb
  pair (`escape`, `click-outside`) would make it scriptable and is the single
  cheapest addition to `--steps` for future rounds.
- **⌘N in each project.** `NewSession` is an action with no step verb.
- **Folding and unfolding a project group.** The group row's chevron is a
  click; `--steps` can set the grouping but not toggle one group. (The
  *library* covers it: `aui/tests/nav_round2.rs` sweeps clicks down the column
  and asserts only the fold row reports.)
- **Unarchiving.** `archive` + `archive-confirm` + `show-archived` exist;
  there is no verb that puts an archived session back.
- **Approvals and questions by click.** `choose:<n>` and `answer:<label>`
  drive the same handlers the buttons call, so the *handler* path is covered;
  the pointer path is not. See J2 and the D1 note below.
- **Any hover state.** No pointer control on this machine.

## J2 — live

**Spent this round: zero billed turns.** The budget was six and none was used.
That is a deliberate choice, not an omission, and the reasoning is worth
recording because it will come up again.

J2's centrepiece is *a prompt that provokes a shell approval → Allow once →
the result arrives*. It exists to prove D1 (the approval buttons). But the
approval half cannot be reached on this machine at all: Muse answers

> tool failed: environment failure: managed shell sandbox is unavailable
> The execution environment is broken: the command was never started and
> every later command will fail the same way

to `session/userShell`, which is the free approval generator, and a billed
turn that asks the model to run a shell command would hit the same broken
sandbox — one turn spent to reproduce an environment failure, not an
approval. Spending three or six turns to watch the same error is not what the
budget was for. **The J2 approval journeys are blocked on the sandbox, not on
the budget**, and the first thing the next round should do is check whether
`session/userShell` runs; if it does, the whole approval journey is free.

### What was verified live, for free

One live run against `muse 1.2.1` (`session/start` + `session/userShell`,
both on the free list — zero `turn/start`, confirmed against Muse's own logs,
§4):

| step | result |
|---|---|
| connect to `muse serve` | **pass** — `harness: connected to muse 1.2.1` |
| `session/start` in a fresh workspace | **pass** — the session opens, the sidebar row appears, the header crumb names the folder |
| account identity in the footer | **pass** — the signed-in address is drawn |
| `session/userShell` | **fail, environmental** — "managed shell sandbox is unavailable" |
| the tier banner | **fail** — see the audit, §8, finding 12: the banner says the plan is unknown in a run begun a minute after `--print-tier` printed it from the same login |

### J2's remainder

Still to drive when the sandbox works, in this order, and all but the first
two are free:

1. a prompt that provokes a shell approval — **one billed turn**
2. Allow once → the result arrives — free (`approval/decide`)
3. a prompt that provokes a question — **one billed turn**
4. answer it → the reply arrives — free (`userInput/answer`)
5. reopen that session from the sidebar — free (`session/resume`, `view/page`)
6. open it from a second harness process → the session-in-use banner — free

## 3. Results

Run 2026-09-13 on `owner-round-3-2026-09-13` (harness `4689591`), debug build,
nothing else running on the machine. **44 passed, 11 failed** — and every one of
the 11 failed on byte-identity alone, not on reaching the wrong place.

### The failures are the clock, not the app

`HARNESS_DETERMINISTIC=1` freezes the clock **per process**, at that process's
first read — it does not freeze it to a *fixed instant*. A journey's two runs
are about 24 s apart (a 12 s screenshot delay each), so any relative label in
the capture ("4h", "now", "1m") sitting on a boundary ticks over between them,
and the pair stops being byte-identical even though both runs reached exactly
the same place.

Verified rather than assumed, twice:

- `sidebar-narrow`, the same steps run **back to back**: byte-identical.
- In the capture suite, `projects-sidebar-dark` from two whole-script runs
  ~20 minutes apart differed in exactly one glyph — a session row reading
  `4h` in one and `5h` in the other. Project order, counts, rows, marks,
  margins and every other pixel matched.

**The fix, for the next round:** a `HARNESS_NOW=<rfc3339>` that pins the frozen
clock to a *given* instant rather than to process start. That makes the whole
capture suite and every journey reproducible across invocations, not only
within one, and it retires the "18 differ by sub-pixel anti-aliasing" caveat
carried since 2026-09-12. It is a few lines in `crate::clock`.

*(Also worth a line: `scripts/journeys.sh`'s FAIL branch prints no capture path
for the `capture_replay` journeys. The captures are written — `toolshapes.png`
and `toolgroup.png` are both there — the report just does not name them.)*

| journey | result | capture |
|---|---|---|
| `hero` | **pass** | `<out>/hero.png` |
| `adopt-one` | **pass** | `<out>/adopt-one.png` |
| `adopt-two` | fail — label drift | `<out>/adopt-two.png` |
| `adopt-three` | **pass** | `<out>/adopt-three.png` |
| `switch-header-menu` | fail — label drift | `<out>/switch-header-menu.png` |
| `switch-palette` | **pass** | `<out>/switch-palette.png` |
| `project-colour` | fail — label drift | `<out>/project-colour.png` |
| `project-menu-named` | fail — label drift | `<out>/project-menu-named.png` |
| `remove-dialog` | **pass** | `<out>/remove-dialog.png` |
| `session-open` | **pass** | `<out>/session-open.png` |
| `session-pin` | **pass** | `<out>/session-pin.png` |
| `session-rename` | **pass** | `<out>/session-rename.png` |
| `session-archive-dialog` | **pass** | `<out>/session-archive-dialog.png` |
| `session-archived` | **pass** | `<out>/session-archived.png` |
| `session-show-archived` | **pass** | `<out>/session-show-archived.png` |
| `session-hidden` | **pass** | `<out>/session-hidden.png` |
| `session-empty` | **pass** | `<out>/session-empty.png` |
| `group-by-date` | fail — label drift | `<out>/group-by-date.png` |
| `group-by-project` | **pass** | `<out>/group-by-project.png` |
| `search-all` | **pass** | `<out>/search-all.png` |
| `search-empty` | **pass** | `<out>/search-empty.png` |
| `menu-overflow` | **pass** | `<out>/menu-overflow.png` |
| `menu-view` | **pass** | `<out>/menu-view.png` |
| `menu-account` | **pass** | `<out>/menu-account.png` |
| `menu-model` | **pass** | `<out>/menu-model.png` |
| `menu-effort` | **pass** | `<out>/menu-effort.png` |
| `menu-mode` | **pass** | `<out>/menu-mode.png` |
| `menu-plus` | **pass** | `<out>/menu-plus.png` |
| `menu-command` | **pass** | `<out>/menu-command.png` |
| `menu-mention` | **pass** | `<out>/menu-mention.png` |
| `palette-command` | **pass** | `<out>/palette-command.png` |
| `sidebar-collapsed` | **pass** | `<out>/sidebar-collapsed.png` |
| `sidebar-narrow` | fail — label drift | `<out>/sidebar-narrow.png` |
| `sidebar-wide` | fail — label drift | `<out>/sidebar-wide.png` |
| `transcript-top` | **pass** | `<out>/transcript-top.png` |
| `transcript-mid` | **pass** | `<out>/transcript-mid.png` |
| `transcript-end` | fail — label drift | `<out>/transcript-end.png` |
| `transcript-groups` | **pass** | `<out>/transcript-groups.png` |
| `composer-draft` | fail — label drift | `<out>/composer-draft.png` |
| `approval-pending` | **pass** | `<out>/approval-pending.png` |
| `approval-chosen` | **pass** | `<out>/approval-chosen.png` |
| `approval-settled` | **pass** | `<out>/approval-settled.png` |
| `question-open` | **pass** | `<out>/question-open.png` |
| `markdown` | **pass** | `<out>/markdown.png` |
| `toolshapes` | fail — label drift | `<out>/toolshapes.png` |
| `toolgroup` | fail — label drift | `<out>/toolgroup.png` |
| `error-retry` | **pass** | `<out>/error-retry.png` |
| `todo-goal` | **pass** | `<out>/todo-goal.png` |
| `reasoning` | **pass** | `<out>/reasoning.png` |
| `empty-session` | **pass** | `<out>/empty-session.png` |
| `finishing` | **pass** | `<out>/finishing.png` |
| `login-choose` | **pass** | `<out>/login-choose.png` |
| `login-device` | **pass** | `<out>/login-device.png` |
| `login-apikey` | **pass** | `<out>/login-apikey.png` |
| `login-error` | **pass** | `<out>/login-error.png` |

## 4. Spend

Counted from Muse's own logs, never from memory:

```sh
grep -c runtime.user_intent.accepted ~/.local/share/muse/sessions/*/*/*/*/session.jsonl \
  | awk -F: '{s+=$2} END {print s}'
```
