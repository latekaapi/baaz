# Section F — Library inventory and merge-back map

Scope: every `aui` component the harness uses (grep `aui::` in `crates/harness/src`),
its gallery reference design, plus the 11 library gaps implied by the owner list.
No screenshots taken (inventory section; nothing rendered).

## Table 1 — aui components the harness uses

Library path root: `/Users/latekaapi/Projects/agentic-ui/crates/aui/src/`.
Gallery registry: `crates/aui-gallery/src/registry.rs`. Harness root: `crates/harness/src/`.

| Harness use (file:line) | Library symbol (file:line) | Gallery entry (reference design) |
| --- | --- | --- |
| `app.rs:34` `app_shell`, `sidebar_header`, `centre_header`, `right_header` | `shell/app_shell.rs`, `shell/header.rs:204` (`centre_header` with `on_overflow`, `right_header`) | `shell/app-shell` (registry.rs:114) |
| `app.rs:31` `sidebar_view`, `sidebar_search`, `sidebar_footer`, `RowAction` | `nav/views.rs:160` (`sidebar_view`), `nav/sidebar.rs:245` (`sidebar`), `nav/session_row.rs:68` (`RowAction`) | `sidebar/sidebar` (registry.rs:~164), `sidebar/views` (registry.rs:~187) |
| `sidebar.rs:14` `DateGroup`, `Grouping`, `SessionSummary`; `sidebar.rs:159` `Grouping::Date` | `nav/views.rs:135` (`Grouping`: Status/Project/Date), `nav/types.rs` (`SessionSummary`) | `sidebar/views` |
| `sidebar.rs:136` `MetaItem::Text` (activity line) | `nav/types.rs` (`MetaItem`) | `sidebar/rows` (registry.rs:~157) |
| `app.rs:1477` `.row_actions([Rename, Hide])`; `app.rs:1470-1471` rename/hide handlers | `nav/session_row.rs:68-86` (`RowAction`: Terminal/Browser/Pin/Rename/Hide/More; `ALL` = Terminal, Browser, Pin, More) | `sidebar/rows` |
| `session.rs:40-44` `composer`, `command_menu`, `mention_picker`, `model_menu`, `mode_menu`, `effort_menu`, `plus_menu`, `queue_strip`, `ComposerChip*`, `PickerRow`, `PlusMenuItem`, `CommandItem/Section`, `MentionItem/Section`, `QueueStripRow` | `composer/composer.rs`, `composer/menus.rs:146` (`command_menu`), `:308` (`mention_picker`), `composer/menu.rs:72` (`plus_menu`), `composer/pickers.rs`, `composer/queue.rs` | `composer/composer` (registry.rs:~294), `composer/menus`, `composer/pickers` |
| `app.rs:27` `composer_state_rows`; `session.rs:2242` `composer(...)` docked | `composer/` state helpers | `composer/composer` |
| `session.rs:1752` `drop_overlay`; `transcript.rs:656` `suggestion_chips` | `composer/composer.rs` (`DropOverlay`), `composer/queue.rs` or `composer.rs` (`SuggestionChips`) | `composer/composer`, `composer/attachments` (registry.rs:~324) |
| `transcript.rs:20-23` `activity_group`, `answered_row`, `approval_card`, `assistant_turn`, `error_card`, `generic_item_card`, `goal_card`, `marker_row`, `plan_card`, `question_card`, `summary_card`, `thinking_block`, `todo_list`, `tool_card`, `user_turn` | `transcript/activity.rs:51` (`activity_group`), `transcript/thinking.rs:42` (`thinking_block`), `transcript/tool_card.rs:96` (`tool_card`), `transcript/todo.rs:52` (`todo_list`), `transcript/question.rs:119` (`question_card`), `transcript/code.rs:96` (`code_block`), `transcript/prose.rs:170` (`prose`) | `transcript/activity`, `transcript/thinking`, `transcript/tool-cards`, `transcript/approval`, `transcript/question-plan-todo`, `transcript/code-diff`, `transcript/summary-status`, `transcript/turns`, `transcript/markers` (registry.rs:204-294) |
| `session.rs:49`, `transcript.rs` via session: `status_row`, `retry_row`, `needs_you_banner`, `format_duration` | `transcript/status.rs`, `transcript/tool_card.rs:124` (`format_duration`) | `transcript/summary-status` |
| `session.rs:46` `ContextMeterState`, `ContextPressure`; `app.rs:28` `button` | `data/context_meter.rs`, `data/button.rs` | `composer/composer` (meter in toolbar), foundations |
| `session.rs:47`, `app.rs:29` `banner`, `BannerKind`, `BannerRun`, `BannerActionStyle` | `feedback/` (`banner`) | `shell/toasts` (registry.rs:~144) |
| `app.rs:2094` `toast_stack`; `overlays.rs:18` `ToastData`, `ToastAction`, `ToastKind` | `feedback/` (`toast_stack`) | `shell/toasts` |
| `app.rs:32` `command_palette`, `dialog`, `popover_layer`, `DialogKind`, `PaletteItem/Section`, `PaletteIcon` | `overlay/command_palette.rs`, `overlay/dialog.rs:124` (`dialog`), `overlay/mod.rs` (`popover_layer`) | `shell/command-palette` (registry.rs:~134), `overlay/dialog` (registry.rs:~154) |
| `session.rs:48` `popover_layer` | `overlay/mod.rs` | (helper, no card) |
| `app.rs:34` `login`, `LoginIntent`, `LoginState` | `screens/login.rs` | `screens/login` (registry.rs:~457) |
| `main.rs:318` `aui::init`; `app.rs:159-164` `keys::ROOT_CONTEXT`; `app.rs:1755` `APPROVAL_CONTEXT`; `app.rs:1855,1926` `MENU_CONTEXT`; `app.rs:2014` `track_pointer`; `app.rs:2035,2039` `set_keyboard_nav`; `app.rs:1728` `ChooseNth`; `app.rs:2031` `TogglePalette` | `lib.rs` (`init`), `keys.rs` (all contexts/actions) | keyboard entries exercised by `crates/aui/tests/keyboard.rs` |
| `main.rs:316` `assets::AuiAssets` | `assets.rs` | `foundations/icons` (registry.rs:~104) |
| via `Cargo.toml:15-19`: `aui-protocol` (`Session`, `Turn`, `Block`, `AttachmentKind`), `aui-tokens` (`scale`), `aui-icons` (`IconName`, `Provider`), `aui-motion` (`stream_reveal` in `transcript.rs`) | `aui-protocol`, `aui-tokens`, `aui-icons`, `aui-motion` crates | `foundations/*`, motion card |

Not determined: whether harness uses `aui::workbench` (terminal/browser/diff/files) anywhere — the `aui::` grep showed no `workbench::` import in `crates/harness/src`; not confirmed whether that is intentional (right-pane scope) or a gap.

## Table 2 — library gaps implied by owner list

Status key: exists / partial (what is missing) / absent. "Home" = where the fix belongs.

| # | Gap (owner item) | Status | What exists (file:line) | What is missing | Home |
| --- | --- | --- | --- | --- | --- |
| 1 | Resizable panes (Sidebar 6) | absent | Shell has fixed columns; `shell/app_shell.rs:163,173` mentions dividers as painted borders, no drag logic; no `resiz\|Resize\|split` drag API in `shell/*.rs` or `util.rs` | Draggable divider, persisted widths, live reflow without jank | library (with gallery entry, e.g. `shell/resize`) — it is a shell primitive every consumer needs |
| 2 | Markdown richness: tables, images, headings (Transcript 4) | partial | `transcript/prose.rs:2` supports only "paragraphs, bullet lists, inline code and bold"; `code.rs:96` (`code_block` + `syntax_runs_in`) covers fenced code separately | Tables, images, headings, richer lists; composition of prose + code blocks for a full message | library (extend `prose` + gallery `transcript/turns`) — rendering is shared design |
| 3 | Selectable text (Transcript 3) | absent | `prose.rs:180,191` renders via `StyledText::with_runs` — gpui `StyledText` is not selectable; no `selectable\|Selectable\|selection` in `prose.rs` | Text selection in transcript turns | library (needs a shared selectable-text primitive + gallery entry); harness cannot fix per-view |
| 4 | Grouped tool cards (Transcript 8) | exists (unwired?) | `transcript/activity.rs:51` `activity_group` ("consecutive quick steps folded into one summary row"); harness imports it (`transcript.rs:21`) and calls it at `transcript.rs:359` | Verify whether harness actually groups consecutive tool calls or still renders single `tool_card`s; if unwired, the gap is harness wiring, not a missing component | harness first (wire `activity_group`); library only if the group header needs a tool-call variant |
| 5 | Reasoning cell (Transcript 9) | exists | `transcript/thinking.rs:42` `thinking_block` (shimmer label, elapsed, collapses to summary); harness renders it (`transcript.rs:348`) | Not determined whether the owner complaint is visibility/wiring vs a missing design; component exists | harness (wire/show), unless design wants a new variant — then library |
| 6 | Dropdown/overflow menu (Header 3; Composer 4-5) | partial | `shell/header.rs:245` `centre_header::on_overflow` (only a click hook — the menu itself is the caller's); `overlay/` has `command_palette`, `dialog`, `view_menu` (`nav/view_menu.rs`); `composer/menu.rs:72` `plus_menu`, `composer/menus.rs:146,308` full-width `command_menu`/`mention_picker` ("at the width of its container") | A generic anchored dropdown menu primitive (header overflow Rename/Fork/Archive; max-width capped slash/@ menus per owner Composer 5) | library (generic menu + gallery entry); harness wires items |
| 7 | Pinned sessions + pinned group (Sidebar 7.1) | partial | `RowAction::Pin` exists (`nav/session_row.rs:68-86`) and is in `ALL`; `Grouping` has only Status/Project/Date (`nav/views.rs:135-142`) — no Pinned variant | Pin action exists; "Pinned N" group at top of list does not | library (pinned grouping in `SidebarView` + `sidebar/views` gallery state); harness owns pin persistence |
| 8 | Inline confirm for archive; archive itself (Sidebar 7.3) | partial/absent | `RowAction::{Rename, Hide}` exist; harness wires Rename/Hide (`app.rs:1470-1477`); `overlay/dialog.rs:124` modal `dialog` exists; `CompactSessionRow::editor` (`session_row.rs:481`) supports inline rename | No `RowAction::Archive`; no inline-confirm pattern (row-local confirm vs modal is a design decision) | library for the inline-confirm pattern + `Archive` action if adopted (gallery `sidebar/rows`); harness for archive semantics |
| 9 | Sidebar collapsed rail (Sidebar 1; Header 5) | exists | `nav/rail.rs:128` `rail` (+ `flat`, `avatar`, `on_select`, `on_action`); `nav/sidebar.rs:252` `Sidebar::collapsed`; gallery `sidebar/sidebar` covers "the collapsed rail" | Owner Sidebar 1 ("collapsed sidebar is empty") looks like harness not feeding `rail_items` (`sidebar.rs:199`) / not rendering `Rail`, not a missing component; Header 5 traffic-light clipping is harness layout | harness (feed `rail_items`, fix widths) |
| 10 | Attachment thumbnails + non-image files (Composer 2-3) | partial | `attachment_row` (`composer/attachments.rs:107`) with `AttachmentKind` (Image/File/Text) and states Ready/Uploading/Failed/Hint; Image kind shows gradient placeholder + glyph (`attachments.rs:173-174`), not a real thumbnail | Real image thumbnails; PDF/md/excel/word rows (glyph + meta only — may be enough, design decision) | library (thumbnail tile + gallery `composer/attachments` states); harness for upload plumbing |
| 11 | Dense input / rename field sizing (Sidebar 8) | not determined | `CompactSessionRow::editor` takes a caller-owned field (`session_row.rs:481`); the oversized rename box is likely the harness-supplied `Textarea` metrics, not the row | Needs Section B/C diagnosis of which element supplies the padding | harness presumptively; library only if row CSS is at fault |

Cross-cutting (not components): scroll jank, session-switch flicker, Enter/Shift+Enter, plus-menu timing, menu+transcript double scroll, menu bar, app icon, Cmd+W/Q — all harness-side, no library home.

## Library gates (verbatim)

From harness `CLAUDE.md:56-63` (library changes):

> Go in `/Users/latekaapi/Projects/agentic-ui` on a **new branch off `main`** (`muse-support`
> is merged), as their own commits, under the library's gates: build, the all-features build
> (`--features aui-webview/wry,aui-terminal/pty,aui-terminal/tui`), test, clippy
> `-D warnings`, rustdoc `-D warnings`, `python3 scripts/api-doc.py`, and a gallery entry for
> anything new. Library rules: no literal colours/sizes/durations, stateless `RenderOnce`
> components with intents out, `popover_layer` for overflow, both themes.

From library `docs/00-agent-brief.md:7` (CI contract, `.github/workflows/ci.yml`):

> `.github/workflows/ci.yml` is the contract: default build, the all-features build (`aui-webview/wry,aui-terminal/pty,aui-terminal/tui`), `cargo test --workspace`, `cargo clippy --workspace -- -D warnings` and `cargo doc --workspace --no-deps` under `RUSTDOCFLAGS=-D warnings` — keep all five green.

Branch rule: new branch off `main` in `/Users/latekaapi/Projects/agentic-ui` (harness `CLAUDE.md:58`); `muse-support` is already merged so no reuse. Harness rule used for reference: no literal colours/sizes/durations (`cx.aui().colors`, `aui_tokens::scale::*`, `cx.aui().metrics`), stateless `RenderOnce`, data in / intents out, `popover_layer` for anything overflowing its box, light + dark both correct, gallery entry for anything new, regenerate `docs/06-api.md` via `cargo doc --workspace --no-deps && python3 scripts/api-doc.py`.

## Dependencies between items

- Gap 6 (generic dropdown) blocks Header 3 overflow menu, Composer 4-5 capped menus, and Gap 8 if archive-confirm uses a menu rather than inline confirm.
- Gap 7 (pinned group) and Gap 8 (`Archive` action) both touch `RowAction`/`Grouping`/`SidebarView` — land as one library change with one gallery update.
- Gap 2 (markdown) and Gap 3 (selectable text) interact: a selectable-text primitive constrains how rich prose runs can be built — design together.
- Gap 4 (grouped tool cards) needs no library work if `activity_group` wiring suffices — confirm before scheduling library time.
- Gap 1 (resizable panes) is independent but the largest (S/M/L: L); everything else is S/M.

## What I ran

- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; pwd; ls /Users/latekaapi/Projects/harness/docs/diagnosis/inputs/ 2>&1 | head -20; ls /Users/latekaapi/Projects/harness/crates/harness/src/ 2>&1 | head -40`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/harness && grep -rhn "aui::[a-z_:]*" crates/harness/src --include="*.rs" | sed 's/^\([^:]*\):\([0-9]*\)://' | grep -o "aui::[A-Za-z_:]*" | sort | uniq -c | sort -rn; echo ---; ls /Users/latekaapi/Projects/agentic-ui/crates/aui/src/; echo ---; ls /Users/latekaapi/Projects/agentic-ui/crates/aui-gallery/src/cards/ 2>/dev/null; ls /Users/latekaapi/Projects/agentic-ui/crates/aui-gallery/src/ 2>/dev/null`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/harness && grep -rn "aui::" crates/harness/src --include="*.rs" | head -80`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/harness && sed -n '40,60p' crates/harness/src/session.rs; echo ===; sed -n '20,40p' crates/harness/src/transcript.rs; echo ===; grep -n "aui\|gpui_kit\|gpui-kit\|use crate" crates/harness/src/session.rs | head -60; echo ===; grep -rn "aui" crates/harness/Cargo.toml`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; ls /Users/latekaapi/Projects/agentic-ui/crates/aui/src/transcript/ /Users/latekaapi/Projects/agentic-ui/crates/aui/src/composer/ /Users/latekaapi/Projects/agentic-ui/crates/aui/src/nav/ /Users/latekaapi/Projects/agentic-ui/crates/aui/src/shell/ /Users/latekaapi/Projects/agentic-ui/crates/aui/src/overlay/ /Users/latekaapi/Projects/agentic-ui/crates/aui/src/screens/ 2>&1; echo ===; ls /Users/latekaapi/Projects/agentic-ui/crates/aui/src/workbench/ 2>&1; ls /Users/latekaapi/Projects/agentic-ui/crates/aui/src/data/ 2>&1`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && git log --oneline -3 2>&1 | head; git status --short 2>&1 | head; echo ===; grep -n "branch\|main\|worktree\|commit" CLAUDE.md 2>/dev/null | head -20; ls docs/`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cat /Users/latekaapi/Projects/agentic-ui/CLAUDE.md 2>/dev/null || ls /Users/latekaapi/Projects/agentic-ui/*.md`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && cat README.md | head -80; echo ===; grep -rn "gates\|clippy\|rustdoc\|api-doc\|gallery entry\|all-features\|new branch" docs/00-agent-brief.md docs/05-handoff.md docs/09-integration-brief.md 2>/dev/null | head -30`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && grep -n "entry\|\"shell\|\"nav\|\"transcript\|\"composer\|\"overlay\|\"screens\|\"workbench\|\"feedback\|\"data" crates/aui-gallery/src/registry.rs | head -100`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && grep -rn "pub fn" crates/aui/src/transcript/prose.rs crates/aui/src/transcript/tool_card.rs crates/aui/src/transcript/thinking.rs crates/aui/src/nav/session_row.rs crates/aui/src/nav/rail.rs crates/aui/src/nav/sidebar.rs crates/aui/src/composer/attachments.rs crates/aui/src/overlay/command_palette.rs 2>/dev/null | head -60`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && grep -rn "RowAction" crates/aui/src/nav/*.rs | head -15; echo ===; grep -n "activity_group\|ActivityGroup\|tool_group\|ToolGroup\|group" crates/aui/src/transcript/activity.rs crates/aui/src/transcript/tool_card.rs crates/aui/src/transcript/mod.rs 2>/dev/null | head -20; echo ===; grep -n "pub fn prose\|pub struct Prose\|table\|code_block\|CodeBlock\|syntax_runs\|ansi_runs" crates/aui/src/transcript/mod.rs crates/aui/src/transcript/code.rs crates/aui/src/transcript/syntax.rs 2>/dev/null | head -25`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && sed -n '60,110p' crates/aui/src/nav/session_row.rs; echo ===; grep -n "with_runs\|StyledText\|selectable\|Selectable\|selection" crates/aui/src/transcript/prose.rs | head; echo ===; grep -rn "resiz\|Resize\|split\|Split\|divider" crates/aui/src/shell/*.rs crates/aui/src/util.rs 2>/dev/null | head -15; echo ===; grep -n "pub fn\|pub struct\|pub enum" crates/aui/src/overlay/dialog.rs | head -20; echo ===; grep -n "AttachmentKind\|pub fn attachment_row\|thumbnail\|Thumb" crates/aui/src/composer/attachments.rs | head -20`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && sed -n '100,200p' crates/aui-gallery/src/registry.rs; echo ===; grep -rn -i "table" crates/aui/src/transcript/*.rs | head -10; echo ===; grep -rn -i "confirm\|inline.*confirm\|destructive" crates/aui/src/nav/*.rs crates/aui/src/overlay/*.rs | head -10`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && sed -n '200,300p' crates/aui-gallery/src/registry.rs`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && grep -n "fn \|enum \|struct " crates/aui/src/transcript/prose.rs | head -20; echo ===; grep -rn "table\|heading\|bullet\|list\|blockquote\|hr\b" crates/aui/src/transcript/prose.rs | head; echo ===; grep -n "Grouping\|Pinned\|pinned\|group" crates/aui/src/nav/views.rs | head -20; echo ===; grep -n "max_width\|MAX_WIDTH\|width" crates/aui/src/composer/menus.rs | head -10`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/harness && grep -n "sidebar_footer\|sidebar_view\|sidebar_search\|session_row\|compact_session_row\|app_shell\|centre_header\|right_header\|sidebar_header\|command_palette\|dialog\|toast_stack\|activity_group\|thinking_block\|todo_list\|question_card\|plan_card\|approval_card\|tool_card\|composer(\|model_menu\|mode_menu\|effort_menu\|plus_menu\|command_menu\|mention_picker\|queue_strip\|suggestion_chips\|attachment_row\|drop_overlay\|banner(\|needs_you_banner\|retry_row\|status_row\|user_turn\|assistant_turn\|code_block\|summary_card\|error_card\|generic_item_card\|goal_card\|marker_row" crates/harness/src/*.rs | head -60`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; cd /Users/latekaapi/Projects/agentic-ui && sed -n '132,160p' crates/aui/src/nav/views.rs; echo ===; grep -n "overflow\|menu\|popover" crates/aui/src/shell/header.rs | head -12; echo ===; cd /Users/latekaapi/Projects/harness && grep -n "activity_group\|thinking_block\|attachment_row\|RowAction::\|Grouping::\|compact_session_row\|session_row(" crates/harness/src/*.rs | head -25`
- `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; mkdir -p /Users/latekaapi/Projects/harness/docs/diagnosis/shots && ls /Users/latekaapi/Projects/harness/docs/diagnosis/`

Read in full: `docs/diagnosis/inputs/owner-issue-list.md`, harness `CLAUDE.md`, library `docs/00-agent-brief.md`, `docs/04-design-rules.md`, `docs/06-api.md` (representative sections). Not read per assignment but partially relevant: harness `docs/05-handoff.md`, `docs/09-handoff-improvements.md` §§4/7, `docs/07-architecture.md` — gap mapping did not require them; flagged where a claim depends on them (Gap 11).
